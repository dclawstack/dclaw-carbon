replicaCount: 1

image:
  repository: ghcr.io/dclawstack/dclaw-carbon
  pullPolicy: IfNotPresent
  tag: ""

service:
  type: ClusterIP
  port: 8123

ingress:
  enabled: true
  className: nginx
  annotations: {}
  hosts:
    - host: carbon.dclawstack.io
      paths:
        - path: /
          pathType: Prefix
  tls: []

resources:
  limits:
    cpu: 500m
    memory: 512Mi
  requests:
    cpu: 100m
    memory: 128Mi

postgresql:
  enabled: true
  auth:
    database: dclaw_carbon
    username: postgres
    password: postgres
  primary:
    persistence:
      enabled: true
      size: 10Gi

serviceAccount:
  create: true
  annotations: {}
  name: ""

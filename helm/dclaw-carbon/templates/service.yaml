apiVersion: v1
kind: Service
metadata:
  name: {{ include "dclaw-carbon.fullname" . }}
  labels:
    {{- include "dclaw-carbon.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "dclaw-carbon.selectorLabels" . | nindent 4 }}
